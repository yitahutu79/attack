## `attack/results/cross_dataset_formal_unsup/`

这里保存当前 formal split / cross-dataset 主线结果。

子目录说明：

- `swat_baselines/`
  - SWaT formal anomaly-detection split 下的基础 baseline 结果

- `swat_tcn/`
  - SWaT formal split 下的 TCN 结果

- `ton_iot_baselines/`
  - TON_IoT formal / limitation 口径下的基础 baseline 结果

- `ton_iot_tcn/`
  - TON_IoT 下的 TCN 结果

- `unsw_nb15_tcn/`
  - UNSW-NB15 官方 train/test split 下的 TCN 结果

说明：

- 这些目录仍被当前论文表格和辅助脚本引用
- 这里保留的是当前主线 formal 结果，不是历史 smoke 或测试目录
