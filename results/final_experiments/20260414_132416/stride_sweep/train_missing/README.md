# `stride_sweep/train_missing/`

这里保存 stride sweep 中补训缺失 stride 的运行结果。

## 当前有效子目录

- `20260414_132839/`
  - 补训 `stride=10,12,20,24`。

这些结果后来和复用的 `stride=8,16,32` 合并到：

```text
attack/results/final_experiments/20260414_132416/stride_sweep/combined/
```
