# `attack/results/final_experiments/`

这里保存最终主线实验输出。

## 当前有效主线

- `20260414_132416/`
  - 当前论文主线最终结果。
  - 包含 stride sweep、alpha sweep、阈值曲线、TCN FPR sweep、2x2 消融和多 seed。

旧的中断或失败 final experiment 已经迁到 `_archive/`，当前目录只保留这一条主线。

当前写论文和做汇报时，如果需要回溯原始数值，优先从这一条主线目录取数据；不要再从 `_archive/` 中的旧 final experiment 目录取主结果。
